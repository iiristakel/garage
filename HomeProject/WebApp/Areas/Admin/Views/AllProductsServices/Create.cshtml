@model BLL.App.DTO.ProductService

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ProductService</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProductForClientId" class="control-label"></label>
                <select asp-for="ProductForClientId" class ="form-control" asp-items="ViewBag.ProductForClientId"></select>
            </div>
            <div class="form-group">
                <label asp-for="WorkObjectId" class="control-label"></label>
                <select asp-for="WorkObjectId" class ="form-control" asp-items="ViewBag.WorkObjectId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
